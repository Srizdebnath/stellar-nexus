[package]
name = "nexus_token"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = "25.0.0"
soroban-token-sdk = "25.0.0"

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
